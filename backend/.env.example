# Copy this file to .env for local development
# On Render: add these as Environment Variables in the service dashboard

# ── REQUIRED ───────────────────────────────────────────────────────────────────
# Set to your Vercel frontend URL after deploying (comma-separated for multiple)
CORS_ORIGINS=https://your-vercel-app.vercel.app

# ── OPTIONAL (all have working defaults — only set to override) ────────────────
# MAX_FILE_SIZE_MB=20
# MAX_ROWS=10000
# CYCLE_TIMEOUT_SECONDS=5.0
# MAX_CYCLES=5000
# FAN_THRESHOLD=10
# SMURF_WINDOW_HOURS=72
# MERCHANT_AMOUNT_CV_THRESHOLD=0.15
# PAYROLL_BATCH_SECONDS=60.0
# SHELL_MAX_TX=3
# SHELL_MAX_CHAIN=6
# MAX_SHELL_CHAINS=1000
# HIGH_VELOCITY_TX_PER_DAY=5.0
# STRUCTURING_THRESHOLD=10000
# STRUCTURING_MARGIN=0.15
# STRUCTURING_MIN_TX=3
